// @unit-test
Процедура Тест_УдалитьВременныйФайл(Фреймворк) Экспорт
	// Дано
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла("txt");
	Запись = Новый ЗаписьТекста(ИмяВременногоФайла);
	Запись.ЗаписатьСтроку("Проверка");
	Запись.Закрыть();
	
	// Когда
	ОбщийМодуль.УдалитьВременныйФайл(ИмяВременногоФайла);
	
	// Тогда
	Файл = Новый Файл(ИмяВременногоФайла);
	Фреймворк.ПроверитьРавенство(Файл.Существует(), Ложь);
КонецПроцедуры